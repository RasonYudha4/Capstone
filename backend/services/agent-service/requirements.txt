# Force CPU-only torch to prevent CUDA packages being pulled ins
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# API
fastapi
uvicorn[standard]
python-multipart

# PDF extraction
pymupdf4llm
pdfplumber
pypdf
python-docx

# Vector store — pick one
chromadb

# Embedding & generation — OpenVINO path
# optimum[openvino] pulls in: optimum, openvino, openvino-tokenizers, openvino-runtime
optimum[openvino]
optimum-intel
transformers          # tokenizer + apply_chat_template + TextIteratorStreamer
numpy

# Config & utilities
pydantic-settings
python-dotenv
tenacity

# Image processing (enricher / signature detection)
Pillow